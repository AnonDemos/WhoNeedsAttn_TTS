# Site settings
title: "Who Needs Attention? Low-latency streaming TTS with cheap state memory"
description: >-
  Listen to audio samples generated by each model for the same set of text inputs.
theme: jekyll-theme-cayman
github:
  is_project_page: false

livereload: true

# Exclude from processing.
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - texts.tsv
